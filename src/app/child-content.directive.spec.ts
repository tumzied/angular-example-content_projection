import { ChildContentDirective } from './child-content.directive';

describe('ChildContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildContentDirective();
    expect(directive).toBeTruthy();
  });
});
